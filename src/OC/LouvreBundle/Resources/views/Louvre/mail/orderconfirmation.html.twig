<!DOCTYPE html>
<html lang="fr">
<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="Billeterie du Louvre" content="">
    <meta name="Guillaume Pruvost" content="">

    <title>{{ "MUSÉE DU LOUVRE PARIS - CONFIRMATION COMMANDE"|trans }}</title>
</head>

<body>
<div class="container-billeterie">

    {# Content #}
    <div class="row row-form-posts">

        <div class="col-md-12">
            <!-- RECAPITULATIF -->
            <div class="card">
                <div class="card-body">
                    <img src="{{ logo }}" alt="Logo Louvre Paris"/>
                    <h2 class="card-title text-center">{{ "CONFIRMATION DE VOTRE COMMANDE"|trans }}</h2>

                    <div class="row">
                        <div class="col-lg-12">
                            <p>{{ "Merci pour votre commande"|trans }}</p>
                            <p>{{ "Numéro de commande"|trans }} : {{ booking.btoken }}</p>
                            <p>{{ "Date de réservation"|trans }} : {{ booking.bookingdate|date('d-m-Y') }}</p>
                            <p>{{ "Nombre de tickets"|trans }} : {{ booking.ticketnumber }}</p>
                            <p>{{ "Prix total"|trans }} : {{ booking.totalprice }}</p>

                            <div>
                                <h3>{{ "Détail de la commande"|trans }} :</h3>

                                <table>
                                    {% for ticket in booking.tickets %}

                                        <tr>
                                            <td class="">{{ ticket.name }} {{ ticket.firstname }},</td>
                                            <td>{{ "Date de naissance"|trans }}
                                                : {{ ticket.dateofbirth|date('d-m-Y') }}</td>
                                        </tr>
                                        <tr>
                                            <td>1 billet {{ ticket.typeticket|trans }} - {{ ticket.price }} € TTC</td>
                                        </tr>

                                    {% endfor %}
                                </table>
                            </div>

                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>

</body>
</html>